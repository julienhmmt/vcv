.devin/
.DS_Store
# Golang binary (root-level output of `task build` -o ../vcv; anchored so it
# does not also ignore source dirs named vcv, e.g. styles/vcv/)
/vcv
# App logs
vcv.log
# test coverage
*.out
# test logs
test-offline.log

# node_modules
node_modules
vendor
build

# vitest coverage output
app/web/frontend/coverage/

# Vite output (kept directory + .gitkeep for go:embed)
app/web/dist/*
!app/web/dist/.gitkeep
app/web/frontend/dist/
.gstack/

# GoReleaser output
/dist/
